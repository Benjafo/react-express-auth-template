describe('Health Check', () => {
    it('should pass', () => {
        expect(true).toBe(true);
    });

    it('should verify basic math operations', () => {
        expect(2 + 2).toBe(4);
    });
});
